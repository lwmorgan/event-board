.season-container.container-fluid
  .divider
    h1 
      span {{season.year}} {{season.name}}
      //- small {{season.events.length}} event{{(season.events.length > 1) ? 's' : ''}}
  include /includes/pages/season/tabset
